<p style="color: green"><%= notice %></p>

<h1>Itens</h1>

<%= link_to "Deletar todos os itens", del_all_items_path, data: { turbo_method: :delete, turbo_confirm: "Você tem certeza?" } %>
<%= render 'import' %>

<div id="items">
  <table class="table">

  <tbody>
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to "", item %>
    </p>
  <% end %>
  </tbody>
  </div>
</div>

<%= link_to "New item", new_item_path %>
